//require dependencies

//middleware

//connect to mongoose and test connection

//
// In my server.js var mongoUri = process.env.MONGODB_URI || 'mongodb://localhost:27017/grocery_app_dev';
//
// mongoose.connect(mongoUri);
//
// Last thing, ensure your port is also abstracted to use process.env.PORT or 3000; port = process.env.PORT || 3000;
//
// app.listen(port);
// console.log('---------------------------------');
// console.log('Server running on port: ' + port);
// console.log('---------------------------------');

//declare port
